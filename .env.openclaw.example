# OpenClaw Gateway Configuration
# Both services run in same Docker container, so use localhost

# OpenClaw Gateway WebSocket URL (localhost since in same container)
OPENCLAW_GATEWAY_URL=ws://localhost:18789

# OpenClaw Authentication Token (auto-generated on first run)
# Will be set by startup script - see start-with-openclaw.sh
OPENCLAW_AUTH_TOKEN=
